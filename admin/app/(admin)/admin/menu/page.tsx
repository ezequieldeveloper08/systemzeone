import { MenuList } from "@/features/menu/components/MenuList"

export const metadata = {
  title: "Cardápio Digital | Painel",
  description: "Gerencie e configure as opções do cardápio do seu restaurante.",
}

export default function AdminMenuPage() {
  return <MenuList />
}
